[project]
name = "workmuch"
version = "0.1.0"
description = "Record active X11 window/program usage over time"
readme = "README"
requires-python = ">=3.10"
dependencies = [
  "python-xlib>=0.33; sys_platform == 'linux'",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
  "pytest>=8.4.1",
]
